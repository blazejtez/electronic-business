{
    "name": "ksiazker-biznes-elektroniczny",
    "version": "1.0.0",
    "description": "Automated tests to check if placing an order works correctly for Ksiazker store made in Prestashop",
    "author": "Sebastian Pietras",
    "scripts": {
        "test": "env HEADLESS=false mocha order_test.spec.js --require mocha-steps",
        "headless_test": "env HEADLESS=true mocha order_test.spec.js --require mocha-steps"
    },
    "dependencies": {
        "selenium-webdriver": "^4.7.0",
        "mocha": "^10.1.0",
        "mocha-steps": "^1.3.0",
        "chance": "^1.1.9",
        "geckodriver": "^3.2.0"
    }
}
